'use client';

// Local Dependencies
import Button from './index';

// Fixture Definition
const ButtonFixture = () => (
  <Button onClick={() => {}} variant='text' disabled={false}>
    Button
  </Button>
);

export default ButtonFixture;
